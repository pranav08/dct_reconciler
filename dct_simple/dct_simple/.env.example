# Anthropic — only needed for judge LLM (Layer 4)
ANTHROPIC_API_KEY=sk-ant-...

# Local extractor model (Layer 1) — only needed if NOT using mock
EXTRACTION_MODEL=mistralai/Mistral-7B-Instruct-v0.3
EXTRACTION_DEVICE=cpu
